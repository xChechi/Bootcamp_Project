package chechi.nino.bootcamp.repository;

import chechi.nino.bootcamp.entity.bar.BarSeat;
import org.springframework.data.jpa.repository.JpaRepository;

public interface BarSeatRepository extends JpaRepository<BarSeat, Integer> {
}
